version: 2

macros:
  - name:  events_agg
    description: Aggregates the different event types per sessions id
    arguments:
     - name: event_type
       description: checkout, page_vie, add_to_cart, package_shipped